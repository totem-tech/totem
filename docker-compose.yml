version: "3.2"

services:
  dev:
    container_name: totem-node
    image: paritytech/ci-linux:974ba3ac-20201006
    working_dir: /var/www/totem-node
    ports:
      - "9944:9944"
    environment:
      - CARGO_HOME=/var/www/totem-node/.cargo
    volumes:
      - .:/var/www/totem-node
      - type: bind
        source: ./.local
        target: /root/.local
    command: bash -c "cargo build --release && ./target/release/totem-node --dev --ws-external"
